# GMI16 recordings

Recorded on 22 September 2026 for GMI16 (Tunis, 25 September 2026).
**Click a title to play it here on GitHub** — the file view has a real player,
with controls and full screen.

### ▶ [The whole loop — 6:12](gmi16-full-demo.mp4)

Prompt, plan, generated code, validation, pull request, merge, registration,
launch. No tool is named in the request: the model works out which analyses
answer the epidemiological question.

### ▶ [The canvas — 1:26](gmi16-drawer.mp4)

The same pipeline drawn by hand instead of described, for anyone who would
rather decide the topology themselves and let the model only compile it.

### ▶ [The results — 1:01](gmi16-results.mp4)

What comes back in the platform: sequence type, annotation, virulence genes.

---

Re-encoded from the originals at CRF 26 (H.264): 567 MB down to 68 MB, with the
resolution and the frame rate untouched — 2734x1536 at 30 fps, and the terminal
text still sharp. GitHub refuses a file over 100 MB, and the original of the
full demo alone was 418 MB.

A `<video>` tag would be stripped from this page: GitHub's Markdown does not
allow it. Inline players exist only for files attached to an issue or a pull
request, which live outside the repository. The links above are the next best
thing, and they keep the videos versioned with the code they show.

The originals are on Drive and, on gtc-gpu2, under `/mnt/data/video/gmi16/`.
